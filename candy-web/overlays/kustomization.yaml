apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../base

configMapGenerator:
  - name: candy-web-config
    files:
      - index.html=index.html
    behavior: replace

